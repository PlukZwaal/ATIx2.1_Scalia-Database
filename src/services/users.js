// const userDao = require('../dao/userDao');

// const usersService = {
//   get: (userId, callback) => {
//     userDao.getCallback(userId, (error, users) => {
//       if (error) {
//         console.error('Service error:', error);
//         return callback(error, undefined);
//       }
//       callback(undefined, users);
//     });
//   },
// };

// module.exports = usersService;